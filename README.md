# Email-Extraction
Basic email extraction toolkit with a CLI and a minimal Flask UI for exporting results.

## Quick start

```bash
pip install -r requirements.txt
python app.py
```

Open `http://localhost:5000` to extract emails and export results to Excel or PDF.
